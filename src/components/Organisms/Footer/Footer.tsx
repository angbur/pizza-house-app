import { FooterContainer } from './Footer.styles';

const Footer = () => {
  return (
    <footer>
        <FooterContainer className='footer'>
        </FooterContainer>
    </footer>
  );
};

export default Footer;